"""0.37 stable-foundation acceptance suite (ROADMAP items 1-21).

Each item is covered by a ``test_sf_NN_*`` function. Optional engines use
``pytest.importorskip``. Item 15 (DataFusion) records an explicit experimental
non-blocking disposition rather than a foundation obligation.
"""
