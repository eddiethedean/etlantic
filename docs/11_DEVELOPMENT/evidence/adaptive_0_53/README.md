---
status: Experimental
---

# Adaptive execution 0.53 qualification

`scripts/check_adaptive_0_53.py` runs the phase 0.53 adaptive execution and
planner campaign. Use `--write` to record a redacted, content-addressed result
under this directory. The evidence contains test command status and hashes,
never source rows, credentials, or process-local execution handles.
